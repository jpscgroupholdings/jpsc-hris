import { Roboto, Merriweather } from "next/font/google";

export const roboto = Roboto({
  variable: "--font-roboto",
});

export const merriwather = Merriweather({
  variable: "--font-merriweather",
});
